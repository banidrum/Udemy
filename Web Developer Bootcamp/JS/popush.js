var colors = ["red", "orange", "yellow"]

//adds element to the end of the array and also returns the length
colors.push("indigo");

// removes the last element

colors.pop()

// removes first element
colors.shift()

//adds to the beginning
colors.unshift()

//check if element is present in the array
array.indexOf()
