var obj = {
  name: "We",
  age: 45,
  isCool: true,
  friends: ["mimi", "bla"],
  add: function(x,y){ //  method inside object
    return x + y;
  }
}

// obj.add(10,5) calls the function inside obj
